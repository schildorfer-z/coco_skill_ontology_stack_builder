-- ============================================================================
-- Layer 1: Physical KG Tables
-- Generated: 2026-04-15 21:38 UTC
-- ============================================================================

USE SCHEMA _SANDBOX_ONTOLOGY_POC.TICKET_METRICS;

CREATE OR REPLACE TABLE KG_NODE (
    NODE_ID         STRING NOT NULL,
    NODE_TYPE       STRING NOT NULL,
    NAME            STRING,
    PROPS           VARIANT,
    TS_INGESTED     TIMESTAMP_NTZ DEFAULT CURRENT_TIMESTAMP(),
    CONSTRAINT PK_KG_NODE PRIMARY KEY (NODE_ID)
) CLUSTER BY (NODE_TYPE);

CREATE OR REPLACE TABLE KG_EDGE (
    EDGE_ID         STRING NOT NULL,
    SRC_ID          STRING NOT NULL,
    DST_ID          STRING NOT NULL,
    EDGE_TYPE       STRING NOT NULL,
    WEIGHT          FLOAT DEFAULT 1.0,
    PROPS           VARIANT,
    EFFECTIVE_START DATE,
    EFFECTIVE_END   DATE,
    TS_INGESTED     TIMESTAMP_NTZ DEFAULT CURRENT_TIMESTAMP(),
    CONSTRAINT PK_KG_EDGE PRIMARY KEY (EDGE_ID)
) CLUSTER BY (EDGE_TYPE, SRC_ID, DST_ID);

-- Load CrmAccount nodes from FUNCTIONAL.CONVERGE.DIM_CRM_ACCOUNT
INSERT INTO KG_NODE (NODE_ID, NODE_TYPE, NAME, PROPS)
SELECT
    CRM_ACCOUNT_ID::STRING AS NODE_ID,
    'CrmAccount' AS NODE_TYPE,
    CRM_ACCOUNT_NAME::STRING AS NAME,
    OBJECT_CONSTRUCT(*) AS PROPS
FROM FUNCTIONAL.CONVERGE.DIM_CRM_ACCOUNT;

-- Load Date nodes from FUNCTIONAL.CONVERGE.DIM_DATE
INSERT INTO KG_NODE (NODE_ID, NODE_TYPE, NAME, PROPS)
SELECT
    MONTH_YEAR::STRING AS NODE_ID,
    'Date' AS NODE_TYPE,
    MONTH_NAME::STRING AS NAME,
    OBJECT_CONSTRUCT(*) AS PROPS
FROM FUNCTIONAL.CONVERGE.DIM_DATE;

-- Load InstanceAccount nodes from FUNCTIONAL.CONVERGE.DIM_INSTANCE_ACCOUNT
INSERT INTO KG_NODE (NODE_ID, NODE_TYPE, NAME, PROPS)
SELECT
    INSTANCE_ACCOUNT_ID::STRING AS NODE_ID,
    'InstanceAccount' AS NODE_TYPE,
    INSTANCE_ACCOUNT_ID::STRING AS NAME,
    OBJECT_CONSTRUCT(*) AS PROPS
FROM FUNCTIONAL.CONVERGE.DIM_INSTANCE_ACCOUNT;

-- Load AggInstanceAgentSummaryMonthly nodes from FUNCTIONAL.CONVERGE.FACT_AGG_INSTANCE_AGENTS_SUMMARY_MONTHLY
INSERT INTO KG_NODE (NODE_ID, NODE_TYPE, NAME, PROPS)
SELECT
    INSTANCE_ACCOUNT_ID::STRING AS NODE_ID,
    'AggInstanceAgentSummaryMonthly' AS NODE_TYPE,
    INSTANCE_ACCOUNT_ID::STRING AS NAME,
    OBJECT_CONSTRUCT(*) AS PROPS
FROM FUNCTIONAL.CONVERGE.FACT_AGG_INSTANCE_AGENTS_SUMMARY_MONTHLY;

-- Load AggInstanceChannelTicketMonthly nodes from FUNCTIONAL.CONVERGE.FACT_AGG_INSTANCE_CHANNEL_TICKETS_MONTHLY
INSERT INTO KG_NODE (NODE_ID, NODE_TYPE, NAME, PROPS)
SELECT
    INSTANCE_ACCOUNT_ID::STRING AS NODE_ID,
    'AggInstanceChannelTicketMonthly' AS NODE_TYPE,
    INSTANCE_ACCOUNT_ID::STRING AS NAME,
    OBJECT_CONSTRUCT(*) AS PROPS
FROM FUNCTIONAL.CONVERGE.FACT_AGG_INSTANCE_CHANNEL_TICKETS_MONTHLY;

-- Load AggInstanceGuideMetricMonthly nodes from FUNCTIONAL.CONVERGE.FACT_AGG_INSTANCE_GUIDE_METRICS_MONTHLY
INSERT INTO KG_NODE (NODE_ID, NODE_TYPE, NAME, PROPS)
SELECT
    INSTANCE_ACCOUNT_ID::STRING AS NODE_ID,
    'AggInstanceGuideMetricMonthly' AS NODE_TYPE,
    INSTANCE_ACCOUNT_ID::STRING AS NAME,
    OBJECT_CONSTRUCT(*) AS PROPS
FROM FUNCTIONAL.CONVERGE.FACT_AGG_INSTANCE_GUIDE_METRICS_MONTHLY;

-- Load AggInstanceRbaChannelMetricMonthly nodes from FUNCTIONAL.CONVERGE.FACT_AGG_INSTANCE_RBA_CHANNEL_METRICS_MONTHLY
INSERT INTO KG_NODE (NODE_ID, NODE_TYPE, NAME, PROPS)
SELECT
    INSTANCE_ACCOUNT_ID::STRING AS NODE_ID,
    'AggInstanceRbaChannelMetricMonthly' AS NODE_TYPE,
    INSTANCE_ACCOUNT_ID::STRING AS NAME,
    OBJECT_CONSTRUCT(*) AS PROPS
FROM FUNCTIONAL.CONVERGE.FACT_AGG_INSTANCE_RBA_CHANNEL_METRICS_MONTHLY;

-- Load AggInstanceRbaMetricMonthly nodes from FUNCTIONAL.CONVERGE.FACT_AGG_INSTANCE_RBA_METRICS_MONTHLY
INSERT INTO KG_NODE (NODE_ID, NODE_TYPE, NAME, PROPS)
SELECT
    INSTANCE_ACCOUNT_ID::STRING AS NODE_ID,
    'AggInstanceRbaMetricMonthly' AS NODE_TYPE,
    INSTANCE_ACCOUNT_ID::STRING AS NAME,
    OBJECT_CONSTRUCT(*) AS PROPS
FROM FUNCTIONAL.CONVERGE.FACT_AGG_INSTANCE_RBA_METRICS_MONTHLY;

-- Load ReferenceAccountLookup nodes from FUNCTIONAL.CONVERGE.REFERENCE_ACCOUNT_LOOKUP
INSERT INTO KG_NODE (NODE_ID, NODE_TYPE, NAME, PROPS)
SELECT
    ACCOUNT_ID::STRING AS NODE_ID,
    'ReferenceAccountLookup' AS NODE_TYPE,
    ACCOUNT_NAME::STRING AS NAME,
    OBJECT_CONSTRUCT(*) AS PROPS
FROM FUNCTIONAL.CONVERGE.REFERENCE_ACCOUNT_LOOKUP;

-- Load TransformGongAccountMap nodes from FUNCTIONAL.CONVERGE.TRANSFORM_GONG_ACCOUNTS_MAP
INSERT INTO KG_NODE (NODE_ID, NODE_TYPE, NAME, PROPS)
SELECT
    CONVERSATION_KEY::STRING AS NODE_ID,
    'TransformGongAccountMap' AS NODE_TYPE,
    CRM_ACCOUNT_NAME::STRING AS NAME,
    OBJECT_CONSTRUCT(*) AS PROPS
FROM FUNCTIONAL.CONVERGE.TRANSFORM_GONG_ACCOUNTS_MAP;

-- Load TransformGongOpportunitieMap nodes from FUNCTIONAL.CONVERGE.TRANSFORM_GONG_OPPORTUNITIES_MAP
INSERT INTO KG_NODE (NODE_ID, NODE_TYPE, NAME, PROPS)
SELECT
    CONVERSATION_KEY::STRING AS NODE_ID,
    'TransformGongOpportunitieMap' AS NODE_TYPE,
    CRM_ACCOUNT_NAME::STRING AS NAME,
    OBJECT_CONSTRUCT(*) AS PROPS
FROM FUNCTIONAL.CONVERGE.TRANSFORM_GONG_OPPORTUNITIES_MAP;

-- Load TransformGongTrackerTaxonomy nodes from FUNCTIONAL.CONVERGE.TRANSFORM_GONG_TRACKER_TAXONOMY
INSERT INTO KG_NODE (NODE_ID, NODE_TYPE, NAME, PROPS)
SELECT
    TRACKER_ID::STRING AS NODE_ID,
    'TransformGongTrackerTaxonomy' AS NODE_TYPE,
    NAME::STRING AS NAME,
    OBJECT_CONSTRUCT(*) AS PROPS
FROM FUNCTIONAL.CONVERGE.TRANSFORM_GONG_TRACKER_TAXONOMY;

-- Load UnifiedGongEvent nodes from FUNCTIONAL.CONVERGE.UNIFIED_GONG_EVENTS
INSERT INTO KG_NODE (NODE_ID, NODE_TYPE, NAME, PROPS)
SELECT
    CONVERSATION_KEY::STRING AS NODE_ID,
    'UnifiedGongEvent' AS NODE_TYPE,
    TITLE::STRING AS NAME,
    OBJECT_CONSTRUCT(*) AS PROPS
FROM FUNCTIONAL.CONVERGE.UNIFIED_GONG_EVENTS;

-- Load UnifiedUservoiceEvent nodes from FUNCTIONAL.CONVERGE.UNIFIED_USERVOICE_EVENTS
INSERT INTO KG_NODE (NODE_ID, NODE_TYPE, NAME, PROPS)
SELECT
    SUGGESTION_ID::STRING AS NODE_ID,
    'UnifiedUservoiceEvent' AS NODE_TYPE,
    FORUM_NAME::STRING AS NAME,
    OBJECT_CONSTRUCT(*) AS PROPS
FROM FUNCTIONAL.CONVERGE.UNIFIED_USERVOICE_EVENTS;

-- Load EdaMlContactActivityDailySnapshot nodes from FUNCTIONAL.EDA_ML_DATA.EDA_ML_CONTACTS_ACTIVITY_DAILY_SNAPSHOT_BCV
INSERT INTO KG_NODE (NODE_ID, NODE_TYPE, NAME, PROPS)
SELECT
    ACTIVITY_ID::STRING AS NODE_ID,
    'EdaMlContactActivityDailySnapshot' AS NODE_TYPE,
    ACTIVITY_ID::STRING AS NAME,
    OBJECT_CONSTRUCT(*) AS PROPS
FROM FUNCTIONAL.EDA_ML_DATA.EDA_ML_CONTACTS_ACTIVITY_DAILY_SNAPSHOT_BCV;

-- Load EdaMlContactDailySnapshot nodes from FUNCTIONAL.EDA_ML_DATA.EDA_ML_CONTACTS_DAILY_SNAPSHOT_BCV
INSERT INTO KG_NODE (NODE_ID, NODE_TYPE, NAME, PROPS)
SELECT
    CRM_ACCOUNT_ID::STRING AS NODE_ID,
    'EdaMlContactDailySnapshot' AS NODE_TYPE,
    CONTACT_TITLE::STRING AS NAME,
    OBJECT_CONSTRUCT(*) AS PROPS
FROM FUNCTIONAL.EDA_ML_DATA.EDA_ML_CONTACTS_DAILY_SNAPSHOT_BCV;

-- Load EdaMlCrmAccountDailySnapshot nodes from FUNCTIONAL.EDA_ML_DATA.EDA_ML_CRM_ACCOUNTS_DAILY_SNAPSHOT_BCV
INSERT INTO KG_NODE (NODE_ID, NODE_TYPE, NAME, PROPS)
SELECT
    CRM_ACCOUNT_SKEY::STRING AS NODE_ID,
    'EdaMlCrmAccountDailySnapshot' AS NODE_TYPE,
    CRM_PRIMARY_OWNER_NAME::STRING AS NAME,
    OBJECT_CONSTRUCT(*) AS PROPS
FROM FUNCTIONAL.EDA_ML_DATA.EDA_ML_CRM_ACCOUNTS_DAILY_SNAPSHOT_BCV;

-- Load EdaMlInstanceAccountDailySnapshot nodes from FUNCTIONAL.EDA_ML_DATA.EDA_ML_INSTANCE_ACCOUNTS_DAILY_SNAPSHOT_BCV
INSERT INTO KG_NODE (NODE_ID, NODE_TYPE, NAME, PROPS)
SELECT
    INSTANCE_ACCOUNT_SKEY::STRING AS NODE_ID,
    'EdaMlInstanceAccountDailySnapshot' AS NODE_TYPE,
    INSTANCE_ACCOUNT_NAME::STRING AS NAME,
    OBJECT_CONSTRUCT(*) AS PROPS
FROM FUNCTIONAL.EDA_ML_DATA.EDA_ML_INSTANCE_ACCOUNTS_DAILY_SNAPSHOT_BCV;

-- Load EdaMlInstanceChannelMonthlySnapshot nodes from FUNCTIONAL.EDA_ML_DATA.EDA_ML_INSTANCE_CHANNELS_MONTHLY_SNAPSHOT_BCV
INSERT INTO KG_NODE (NODE_ID, NODE_TYPE, NAME, PROPS)
SELECT
    INSTANCE_ACCOUNT_CHANNEL_SKEY::STRING AS NODE_ID,
    'EdaMlInstanceChannelMonthlySnapshot' AS NODE_TYPE,
    INSTANCE_ACCOUNT_CHANNEL_SKEY::STRING AS NAME,
    OBJECT_CONSTRUCT(*) AS PROPS
FROM FUNCTIONAL.EDA_ML_DATA.EDA_ML_INSTANCE_CHANNELS_MONTHLY_SNAPSHOT_BCV;

-- Load EdaMlInstanceProductDailySnapshot nodes from FUNCTIONAL.EDA_ML_DATA.EDA_ML_INSTANCE_PRODUCTS_DAILY_SNAPSHOT_BCV
INSERT INTO KG_NODE (NODE_ID, NODE_TYPE, NAME, PROPS)
SELECT
    PRODUCT_SKEY::STRING AS NODE_ID,
    'EdaMlInstanceProductDailySnapshot' AS NODE_TYPE,
    PRODUCT_SKEY::STRING AS NAME,
    OBJECT_CONSTRUCT(*) AS PROPS
FROM FUNCTIONAL.EDA_ML_DATA.EDA_ML_INSTANCE_PRODUCTS_DAILY_SNAPSHOT_BCV;

-- Load EdaMlOpportunitieDailySnapshot nodes from FUNCTIONAL.EDA_ML_DATA.EDA_ML_OPPORTUNITIES_DAILY_SNAPSHOT_BCV
INSERT INTO KG_NODE (NODE_ID, NODE_TYPE, NAME, PROPS)
SELECT
    CRM_OPPORTUNITY_SKEY::STRING AS NODE_ID,
    'EdaMlOpportunitieDailySnapshot' AS NODE_TYPE,
    OPPORTUNITY_NAME::STRING AS NAME,
    OBJECT_CONSTRUCT(*) AS PROPS
FROM FUNCTIONAL.EDA_ML_DATA.EDA_ML_OPPORTUNITIES_DAILY_SNAPSHOT_BCV;

-- Load EdaMlUserDailySnapshot nodes from FUNCTIONAL.EDA_ML_DATA.EDA_ML_USERS_DAILY_SNAPSHOT_BCV
INSERT INTO KG_NODE (NODE_ID, NODE_TYPE, NAME, PROPS)
SELECT
    CRM_USER_SKEY::STRING AS NODE_ID,
    'EdaMlUserDailySnapshot' AS NODE_TYPE,
    USER_NAME::STRING AS NAME,
    OBJECT_CONSTRUCT(*) AS PROPS
FROM FUNCTIONAL.EDA_ML_DATA.EDA_ML_USERS_DAILY_SNAPSHOT_BCV;

-- Load EdaMlUserAccountMapping nodes from FUNCTIONAL.EDA_ML_DATA.EDA_ML_USER_ACCOUNT_MAPPING_BCV
INSERT INTO KG_NODE (NODE_ID, NODE_TYPE, NAME, PROPS)
SELECT
    CRM_ACCOUNT_ID::STRING AS NODE_ID,
    'EdaMlUserAccountMapping' AS NODE_TYPE,
    USER_NAME::STRING AS NAME,
    OBJECT_CONSTRUCT(*) AS PROPS
FROM FUNCTIONAL.EDA_ML_DATA.EDA_ML_USER_ACCOUNT_MAPPING_BCV;

-- Load has_crm_account edges from FUNCTIONAL.CONVERGE.DIM_INSTANCE_ACCOUNT
INSERT INTO KG_EDGE (EDGE_ID, SRC_ID, DST_ID, EDGE_TYPE)
SELECT
    UUID_STRING() AS EDGE_ID,
    CRM_ACCOUNT_ID::STRING AS SRC_ID,
    CRM_ACCOUNT_ID::STRING AS DST_ID,
    'has_crm_account' AS EDGE_TYPE
FROM FUNCTIONAL.CONVERGE.DIM_INSTANCE_ACCOUNT;

-- Load has_instance_account edges from FUNCTIONAL.CONVERGE.FACT_AGG_INSTANCE_AGENTS_SUMMARY_MONTHLY
INSERT INTO KG_EDGE (EDGE_ID, SRC_ID, DST_ID, EDGE_TYPE)
SELECT
    UUID_STRING() AS EDGE_ID,
    INSTANCE_ACCOUNT_ID::STRING AS SRC_ID,
    INSTANCE_ACCOUNT_ID::STRING AS DST_ID,
    'has_instance_account' AS EDGE_TYPE
FROM FUNCTIONAL.CONVERGE.FACT_AGG_INSTANCE_AGENTS_SUMMARY_MONTHLY;

-- Load has_crm_account edges from FUNCTIONAL.CONVERGE.FACT_AGG_INSTANCE_AGENTS_SUMMARY_MONTHLY
INSERT INTO KG_EDGE (EDGE_ID, SRC_ID, DST_ID, EDGE_TYPE)
SELECT
    UUID_STRING() AS EDGE_ID,
    CRM_ACCOUNT_ID::STRING AS SRC_ID,
    CRM_ACCOUNT_ID::STRING AS DST_ID,
    'has_crm_account' AS EDGE_TYPE
FROM FUNCTIONAL.CONVERGE.FACT_AGG_INSTANCE_AGENTS_SUMMARY_MONTHLY;

-- Load belongs_to_date edges from FUNCTIONAL.CONVERGE.FACT_AGG_INSTANCE_AGENTS_SUMMARY_MONTHLY
INSERT INTO KG_EDGE (EDGE_ID, SRC_ID, DST_ID, EDGE_TYPE)
SELECT
    UUID_STRING() AS EDGE_ID,
    MONTH_YEAR::STRING AS SRC_ID,
    MONTH_YEAR::STRING AS DST_ID,
    'belongs_to_date' AS EDGE_TYPE
FROM FUNCTIONAL.CONVERGE.FACT_AGG_INSTANCE_AGENTS_SUMMARY_MONTHLY;

-- Load has_instance_account edges from FUNCTIONAL.CONVERGE.FACT_AGG_INSTANCE_CHANNEL_TICKETS_MONTHLY
INSERT INTO KG_EDGE (EDGE_ID, SRC_ID, DST_ID, EDGE_TYPE)
SELECT
    UUID_STRING() AS EDGE_ID,
    INSTANCE_ACCOUNT_ID::STRING AS SRC_ID,
    INSTANCE_ACCOUNT_ID::STRING AS DST_ID,
    'has_instance_account' AS EDGE_TYPE
FROM FUNCTIONAL.CONVERGE.FACT_AGG_INSTANCE_CHANNEL_TICKETS_MONTHLY;

-- Load has_crm_account edges from FUNCTIONAL.CONVERGE.FACT_AGG_INSTANCE_CHANNEL_TICKETS_MONTHLY
INSERT INTO KG_EDGE (EDGE_ID, SRC_ID, DST_ID, EDGE_TYPE)
SELECT
    UUID_STRING() AS EDGE_ID,
    CRM_ACCOUNT_ID::STRING AS SRC_ID,
    CRM_ACCOUNT_ID::STRING AS DST_ID,
    'has_crm_account' AS EDGE_TYPE
FROM FUNCTIONAL.CONVERGE.FACT_AGG_INSTANCE_CHANNEL_TICKETS_MONTHLY;

-- Load belongs_to_date edges from FUNCTIONAL.CONVERGE.FACT_AGG_INSTANCE_CHANNEL_TICKETS_MONTHLY
INSERT INTO KG_EDGE (EDGE_ID, SRC_ID, DST_ID, EDGE_TYPE)
SELECT
    UUID_STRING() AS EDGE_ID,
    MONTH_YEAR::STRING AS SRC_ID,
    MONTH_YEAR::STRING AS DST_ID,
    'belongs_to_date' AS EDGE_TYPE
FROM FUNCTIONAL.CONVERGE.FACT_AGG_INSTANCE_CHANNEL_TICKETS_MONTHLY;

-- Load has_instance_account edges from FUNCTIONAL.CONVERGE.FACT_AGG_INSTANCE_GUIDE_METRICS_MONTHLY
INSERT INTO KG_EDGE (EDGE_ID, SRC_ID, DST_ID, EDGE_TYPE)
SELECT
    UUID_STRING() AS EDGE_ID,
    INSTANCE_ACCOUNT_ID::STRING AS SRC_ID,
    INSTANCE_ACCOUNT_ID::STRING AS DST_ID,
    'has_instance_account' AS EDGE_TYPE
FROM FUNCTIONAL.CONVERGE.FACT_AGG_INSTANCE_GUIDE_METRICS_MONTHLY;

-- Load has_crm_account edges from FUNCTIONAL.CONVERGE.FACT_AGG_INSTANCE_GUIDE_METRICS_MONTHLY
INSERT INTO KG_EDGE (EDGE_ID, SRC_ID, DST_ID, EDGE_TYPE)
SELECT
    UUID_STRING() AS EDGE_ID,
    CRM_ACCOUNT_ID::STRING AS SRC_ID,
    CRM_ACCOUNT_ID::STRING AS DST_ID,
    'has_crm_account' AS EDGE_TYPE
FROM FUNCTIONAL.CONVERGE.FACT_AGG_INSTANCE_GUIDE_METRICS_MONTHLY;

-- Load belongs_to_date edges from FUNCTIONAL.CONVERGE.FACT_AGG_INSTANCE_GUIDE_METRICS_MONTHLY
INSERT INTO KG_EDGE (EDGE_ID, SRC_ID, DST_ID, EDGE_TYPE)
SELECT
    UUID_STRING() AS EDGE_ID,
    MONTH_YEAR::STRING AS SRC_ID,
    MONTH_YEAR::STRING AS DST_ID,
    'belongs_to_date' AS EDGE_TYPE
FROM FUNCTIONAL.CONVERGE.FACT_AGG_INSTANCE_GUIDE_METRICS_MONTHLY;

-- Load has_instance_account edges from FUNCTIONAL.CONVERGE.FACT_AGG_INSTANCE_RBA_CHANNEL_METRICS_MONTHLY
INSERT INTO KG_EDGE (EDGE_ID, SRC_ID, DST_ID, EDGE_TYPE)
SELECT
    UUID_STRING() AS EDGE_ID,
    INSTANCE_ACCOUNT_ID::STRING AS SRC_ID,
    INSTANCE_ACCOUNT_ID::STRING AS DST_ID,
    'has_instance_account' AS EDGE_TYPE
FROM FUNCTIONAL.CONVERGE.FACT_AGG_INSTANCE_RBA_CHANNEL_METRICS_MONTHLY;

-- Load has_crm_account edges from FUNCTIONAL.CONVERGE.FACT_AGG_INSTANCE_RBA_CHANNEL_METRICS_MONTHLY
INSERT INTO KG_EDGE (EDGE_ID, SRC_ID, DST_ID, EDGE_TYPE)
SELECT
    UUID_STRING() AS EDGE_ID,
    CRM_ACCOUNT_ID::STRING AS SRC_ID,
    CRM_ACCOUNT_ID::STRING AS DST_ID,
    'has_crm_account' AS EDGE_TYPE
FROM FUNCTIONAL.CONVERGE.FACT_AGG_INSTANCE_RBA_CHANNEL_METRICS_MONTHLY;

-- Load belongs_to_date edges from FUNCTIONAL.CONVERGE.FACT_AGG_INSTANCE_RBA_CHANNEL_METRICS_MONTHLY
INSERT INTO KG_EDGE (EDGE_ID, SRC_ID, DST_ID, EDGE_TYPE)
SELECT
    UUID_STRING() AS EDGE_ID,
    MONTH_YEAR::STRING AS SRC_ID,
    MONTH_YEAR::STRING AS DST_ID,
    'belongs_to_date' AS EDGE_TYPE
FROM FUNCTIONAL.CONVERGE.FACT_AGG_INSTANCE_RBA_CHANNEL_METRICS_MONTHLY;

-- Load has_instance_account edges from FUNCTIONAL.CONVERGE.FACT_AGG_INSTANCE_RBA_METRICS_MONTHLY
INSERT INTO KG_EDGE (EDGE_ID, SRC_ID, DST_ID, EDGE_TYPE)
SELECT
    UUID_STRING() AS EDGE_ID,
    INSTANCE_ACCOUNT_ID::STRING AS SRC_ID,
    INSTANCE_ACCOUNT_ID::STRING AS DST_ID,
    'has_instance_account' AS EDGE_TYPE
FROM FUNCTIONAL.CONVERGE.FACT_AGG_INSTANCE_RBA_METRICS_MONTHLY;

-- Load has_crm_account edges from FUNCTIONAL.CONVERGE.FACT_AGG_INSTANCE_RBA_METRICS_MONTHLY
INSERT INTO KG_EDGE (EDGE_ID, SRC_ID, DST_ID, EDGE_TYPE)
SELECT
    UUID_STRING() AS EDGE_ID,
    CRM_ACCOUNT_ID::STRING AS SRC_ID,
    CRM_ACCOUNT_ID::STRING AS DST_ID,
    'has_crm_account' AS EDGE_TYPE
FROM FUNCTIONAL.CONVERGE.FACT_AGG_INSTANCE_RBA_METRICS_MONTHLY;

-- Load belongs_to_date edges from FUNCTIONAL.CONVERGE.FACT_AGG_INSTANCE_RBA_METRICS_MONTHLY
INSERT INTO KG_EDGE (EDGE_ID, SRC_ID, DST_ID, EDGE_TYPE)
SELECT
    UUID_STRING() AS EDGE_ID,
    MONTH_YEAR::STRING AS SRC_ID,
    MONTH_YEAR::STRING AS DST_ID,
    'belongs_to_date' AS EDGE_TYPE
FROM FUNCTIONAL.CONVERGE.FACT_AGG_INSTANCE_RBA_METRICS_MONTHLY;

-- Load has_conversation edges from FUNCTIONAL.CONVERGE.TRANSFORM_GONG_ACCOUNTS_MAP
INSERT INTO KG_EDGE (EDGE_ID, SRC_ID, DST_ID, EDGE_TYPE)
SELECT
    UUID_STRING() AS EDGE_ID,
    CONVERSATION_KEY::STRING AS SRC_ID,
    CONVERSATION_KEY::STRING AS DST_ID,
    'has_conversation' AS EDGE_TYPE
FROM FUNCTIONAL.CONVERGE.TRANSFORM_GONG_ACCOUNTS_MAP;

-- Load has_crm_account edges from FUNCTIONAL.CONVERGE.TRANSFORM_GONG_ACCOUNTS_MAP
INSERT INTO KG_EDGE (EDGE_ID, SRC_ID, DST_ID, EDGE_TYPE)
SELECT
    UUID_STRING() AS EDGE_ID,
    CRM_ACCOUNT_ID::STRING AS SRC_ID,
    CRM_ACCOUNT_ID::STRING AS DST_ID,
    'has_crm_account' AS EDGE_TYPE
FROM FUNCTIONAL.CONVERGE.TRANSFORM_GONG_ACCOUNTS_MAP;

-- Load has_conversation edges from FUNCTIONAL.CONVERGE.TRANSFORM_GONG_OPPORTUNITIES_MAP
INSERT INTO KG_EDGE (EDGE_ID, SRC_ID, DST_ID, EDGE_TYPE)
SELECT
    UUID_STRING() AS EDGE_ID,
    CONVERSATION_KEY::STRING AS SRC_ID,
    CONVERSATION_KEY::STRING AS DST_ID,
    'has_conversation' AS EDGE_TYPE
FROM FUNCTIONAL.CONVERGE.TRANSFORM_GONG_OPPORTUNITIES_MAP;

-- Load has_crm_account edges from FUNCTIONAL.CONVERGE.TRANSFORM_GONG_OPPORTUNITIES_MAP
INSERT INTO KG_EDGE (EDGE_ID, SRC_ID, DST_ID, EDGE_TYPE)
SELECT
    UUID_STRING() AS EDGE_ID,
    CRM_ACCOUNT_ID::STRING AS SRC_ID,
    CRM_ACCOUNT_ID::STRING AS DST_ID,
    'has_crm_account' AS EDGE_TYPE
FROM FUNCTIONAL.CONVERGE.TRANSFORM_GONG_OPPORTUNITIES_MAP;

-- Load has_crm_account edges from FUNCTIONAL.CONVERGE.UNIFIED_USERVOICE_EVENTS
INSERT INTO KG_EDGE (EDGE_ID, SRC_ID, DST_ID, EDGE_TYPE)
SELECT
    UUID_STRING() AS EDGE_ID,
    CRM_ACCOUNT_ID::STRING AS SRC_ID,
    CRM_ACCOUNT_ID::STRING AS DST_ID,
    'has_crm_account' AS EDGE_TYPE
FROM FUNCTIONAL.CONVERGE.UNIFIED_USERVOICE_EVENTS;

-- Load agginstanceagentdetailmonthly edges from FUNCTIONAL.CONVERGE.FACT_AGG_INSTANCE_AGENTS_DETAIL_MONTHLY
INSERT INTO KG_EDGE (EDGE_ID, SRC_ID, DST_ID, EDGE_TYPE)
SELECT
    UUID_STRING() AS EDGE_ID,
    INSTANCE_ACCOUNT_ID::STRING AS SRC_ID,
    CRM_ACCOUNT_ID::STRING AS DST_ID,
    'agginstanceagentdetailmonthly' AS EDGE_TYPE
FROM FUNCTIONAL.CONVERGE.FACT_AGG_INSTANCE_AGENTS_DETAIL_MONTHLY;
